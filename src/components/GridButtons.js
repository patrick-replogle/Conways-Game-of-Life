import React from "react";

const GridButtons = (props) => {
  return (
    <div>
      <button>Start</button>
      <button>Pause</button>
      <button>Clear</button>
    </div>
  );
};

export default GridButtons;
