import React from "react";
import GridButtons from "./GridButtons";

const Grid = (props) => {
  return (
    <div>
      <p>grid component</p>
      <GridButtons />
    </div>
  );
};

export default Grid;
